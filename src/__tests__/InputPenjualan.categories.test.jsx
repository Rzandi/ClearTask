import { describe, it, expect } from 'vitest';

describe('InputPenjualan.categories.test.jsx', () => {
  it.skip('Kategori inline input is now handled by Inventory Manager. Skipping obsolete tests.', () => {
    expect(true).toBe(true);
  });
});
